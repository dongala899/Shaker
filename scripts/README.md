# Shaker Scripts

Packaging and install/upgrade scripts have been removed.

Use the app directly from the project folder:

1. Open [login.html](/c:/my%20projects/Shaker/login.html)
2. Sign in
3. Use `Backup Data` and `Restore Data` from inside the app when moving data

Optional localhost helpers are still available in this folder, but they are no longer required for normal use.
